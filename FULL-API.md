# Octava Admin (Next.js + Tailwind + shadcn-ready)

Админ-панель для Octava на **Next.js App Router**, с Tailwind и заготовкой для компонентов **shadcn/ui**.

> API соответствует документам `ADMIN.md` и `README.md` из ТЗ. Главные сценарии: аутентификация, загрузка файлов, CRUD каталога, обновление страниц, лиды.

## Быстрый старт

```bash
pnpm i
cp .env.example .env
# отредактируйте NEXT_PUBLIC_BACKEND_URL (например: http://localhost:3005)
pnpm dev
```

Откройте http://localhost:3000

- `/login` — вход (использует `POST /auth/login`, ставит cookies `accessToken`/`refreshToken`).
- `/admin` — защищённая область (middleware редиректит на `/login`, если нет `accessToken`).

## Архитектура токенов

- `accessToken` (15 минут) и `refreshToken` (30 дней) хранятся в **HttpOnly** cookies.
- Серверная обёртка `lib/server-fetch.ts` автоматически пробует `POST /auth/refresh` при 401 и повторяет запрос один раз.
- Клиент **не видит** токены; все защищённые вызовы делайте через наши `/api/*` прокси-роуты.

## Реализованные разделы

- **Вход /logout**
- **Категории**: список (публичный `GET /service-categories`), создание `POST /admin/catalog/categories` с HERO-изображением.
- **Лиды**: таблица с фильтром поиска и сменой статуса `PATCH /admin/leads/:id/status`.
- **Организация**: форма `PUT /admin/org`.

Заготовки:
- **Услуги**: список по выбранной категории (публичный `GET /service-categories/:slug`). Кнопку «Добавить» подключите к `POST /admin/catalog/services`. Редактирование — через `PUT /admin/catalog/services/:id`.
- **Страницы**: формы под `PUT /admin/pages/*` (ответ 204).

## Загрузка файлов (HERO/галереи)

Компонент `components/file-uploader.tsx` отправляет `multipart/form-data` на `/api/admin/files/upload`, далее проксируется в `POST /admin/files/upload`. Используйте возвращаемый `id` в полях `heroImageFileId`/`galleryImageFileIds`/`seo.ogImageId` (см. ТЗ).

## shadcn/ui

В проекте предустановлен Tailwind и `components.json`. Подключите полный набор компонентов:

```bash
# (опционально) инициализируйте shadcn
npx shadcn@latest init

# добавьте нужные компоненты (или все подряд)
npx shadcn@latest add button input label textarea card toast dialog select table badge tabs separator dropdown-menu sheet
```

В репозитории уже есть лёгкие аналоги базовых компонентов в `components/ui/*`, чтобы всё работало сразу. Вы можете заменить их на сгенерированные shadcn-компоненты.

## Важные нюансы из ТЗ

- Slug генерируется **на бэкенде** (не отправляйте его при создании/обновлении). Возвращается в ответах и используется только для ссылок.  
- Для HERO/галерей: `undefined` — оставить как есть, `null` — удалить, число/массив — пересоздать (порядок важен).  
- После обновлений можно подтягивать публичные карточки (категории/услуги/аппараты/страницы) для предпросмотра.  
- Лиды фильтруются по `status`, `sourceType`, `search`, `from`/`to` и т.д.

## Что дальше

- Добавьте CRUD для **аппаратов** (`/admin/catalog/devices`) по аналогии с услугами/категориями.
- Реализуйте формы для **страниц** (`/admin/pages/*`) и SEO-полей.
- Добавьте медиа-пикеры для галерей и выделение HERO (через наш `FileUploader`).

Удачи! 🚀
